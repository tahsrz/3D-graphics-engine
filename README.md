# 3D Graphics Engine
A 3D graphics engine created in Java. The project was ported to Javascript with these key principles:
engine.Manager -> useFrame
  useFrame((state, delta) => {
  // 60 times per second
  meshRef.current.rotation.y += delta * 0.5; 

SunsetPulse.
});

